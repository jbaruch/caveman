# OAuth2 Flow Selection

## Task

You're building three applications that need to authenticate with a third-party API:
1. A server-side web app (Node.js/Express)
2. A single-page app (React, no backend)
3. A CLI tool that developers run locally

Which OAuth2 flow should each use and why? What are the security considerations for each?
