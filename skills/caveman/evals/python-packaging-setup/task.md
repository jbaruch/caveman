# Python Package Setup

## Task

Set up a Python package with a `pyproject.toml` that: uses setuptools as the build backend, specifies Python >=3.10, declares dependencies on `requests` and `click`, includes a CLI entry point `mycli` that runs `mypackage.cli:main`, and configures pytest as the test runner.
