# Kubernetes Health Probes

## Task

Configure liveness, readiness, and startup probes for a Java Spring Boot application deployed in Kubernetes. The app takes ~30 seconds to start, connects to a database, and occasionally has long GC pauses.
