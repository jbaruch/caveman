# React Component Re-rendering

## Problem

A React component re-renders on every parent state update even though its props haven't changed. The developer is passing an object as a prop.

Explain why this happens and how to fix it.
